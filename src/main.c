#include "../headers/cube3D.h"


//main swann
/* int main(int argc, char **argv)
{
    t_scene scene;

    (void)argc;
    scene = parse_file(argv[1]);

    print_scene(&scene);
    free_scene(&scene);

    return (0);
} */